---
export interface Props {
  icon?: string;
  title?: string;
  description?: string;
}

const { title = await Astro.slots.render('title'), description = await Astro.slots.render('description') } =
  Astro.props;
---

<section class="bg-blue-50 dark:bg-slate-800 not-prose">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-4 text-sm text-center font-medium dark:text-gray-400">
    <!-- optional icon -->
    <!-- <Icon name={icon} class="w-5 h-5 inline-block align-text-bottom font-bold" /> -->

    {title && <span class="font-bold">{title}</span>}

    {description ? <Fragment set:html={description} /> : <slot />}
  </div>
</section>
